```jsx
<div id="cacsaca" style={{ height: 200, overflow: 'auto', position: 'relative' }}>
  <div>
    {new Array(20).fill(0).map((_, i) => (
      <p key={i}>data {i}</p>
    ))}
  </div>
  <BackTop
    visibilityHeight={200}
    target={() => document.querySelector('#cacsaca') || document.body}
    getPopupContainer={(node) => node}
  />
</div>
```