export const basic = {
  type: '组件',
  title: '回到顶部',
  subtitle: 'BackTop',
};
import Box from '@/components/box';
import Snapshot from '@/components/snapshot';
import api from './examples/api.md?raw';

<Box>
# BackTop 回到顶部

> 返回页面顶部的操作按钮。

## 代码演示

<Snapshot name="back-top" col={1} />
</Box>

<Box text={api} />
