export const basic = {
  type: '@moneko',
  title: '说明及注意事项',
  icon: '😋',
};

#### 目录结构说明

import CodeBlock from '@/components/code';
import structure from './examples/structure.txt';

<CodeBlock code={structure} lang="treeview" />

import MBox from '@/components/markdown';
import readme from './examples/readme.md?raw';

<MBox text={readme} getAnchorContainer={() => document.querySelector('main article')} />
