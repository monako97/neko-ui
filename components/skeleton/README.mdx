export const basic = {
  type: '反馈',
  title: '骨架屏',
  subtitle: 'Skeleton',
};
import Box from '@/components/box';
import Snapshot from '@/components/snapshot';
import api from './examples/api.md?raw';

<Box>
# 骨架屏

> 网络较慢，需要长时间等待加载处理的情况下、图文信息内容较多的列表/卡片中、第一次加载数据的时候使用

<Snapshot name="skeleton" col={1} />
</Box>

<Box text={api} />
