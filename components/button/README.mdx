---
type: 通用
title: 按钮
subtitle: n-button
icon: 💠
---

# 按钮

按钮用于开始一个即时操作，响应用户点击行为，触发相应的业务逻辑。

## 注册组件 `只需注册一次`

```js
import { Button, registry } 'neko-ui';

registry(Button);
```
