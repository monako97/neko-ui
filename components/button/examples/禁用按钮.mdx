<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
  {['default', 'primary', 'success', 'warning', 'error'].map((item) => {
    return (
      <Button key={item} disabled type={item}>
        disabled
      </Button>
    );
  })}
</div>
