<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
  {['default', 'primary', 'success', 'warning', 'error'].map((item) => {
    return (
      <Button key={item} circle type={item}>
        🤔
      </Button>
    );
  })}
</div>
