<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
  <Button dashed ghost>
    虚线
  </Button>
  <Button type="primary" dashed ghost>
    虚线
  </Button>
  <Button type="success" dashed ghost>
    虚线
  </Button>
  <Button type="warning" dashed ghost>
    虚线
  </Button>
  <Button type="error" dashed ghost>
    虚线
  </Button>
</div>
