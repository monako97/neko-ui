通过设置 `min` 来约束最小值
通过设置 `max` 来约束最大值

```tsx
function Demo() {
  const [val, setVal] = React.useState(0);

  return (
    <InputNumber
      label="范围"
      placeholder="请输入0~100以内的数值"
      min={0}
      max={100}
      value={val}
      onChange={setVal}
    />
  );
}
```
