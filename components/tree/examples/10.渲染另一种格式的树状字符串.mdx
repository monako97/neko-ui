渲染 tree 命令输出的树状字符串

```jsx
function Demo() {
const data = `.
├── components
│   ├── @moneko
│   │   ├── README.mdx
│   │   └── 🕸sso
│   │       ├── README.mdx
│   │       └── examples
│   │           └── readme.md
│   └── index.ts
├── config
│   ├── deploy.ts
│   └── index.ts
├── jest.config.ts
├── package.json
├── tsconfig.json
└── typings
    └── typings.d.ts`

  return (
    <Tree
      data={data}
      className="treeview-part"
      renderRow={(item, title, subTitle) => {
        const ext =
          item.title.endsWith('/') || item.children ? 'dir' : item.title.match(/[^.]+\w*$/)?.[0];

        return [
          <span key="title" className={`entry-name ext-${ext} ${ext}`}>
            {title}
          </span>,
          subTitle,
        ];
      }}
    />
  );
}
```
