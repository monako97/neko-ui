操作仍在执行中

```tsx
function Demo() {
  const [loading, setLoading] = React.useState(true);

  return (
    <div>
      <Switch onLabel="开启" offLabel="关闭" value={loading} onChange={setLoading} />
      <br />
      <Switch value={true} loading={loading} />
      <br />
      <Switch onLabel="开" offLabel="关" loading={loading} />
    </div>
  );
}
```
