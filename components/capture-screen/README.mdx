export const basic = {
  type: '组件',
  title: '捕获屏幕',
  subtitle: 'CaptureScreen',
};

# 捕获、录制屏幕

> 捕获、录制屏幕

import Snapshot from '@/components/snapshot';

## 捕获屏幕

<Snapshot path="capture-screen/examples/default" lang="tsx" />

## 录制屏幕并保存到本地

<Snapshot path="capture-screen/examples/recorder" lang="tsx" />

## 捕获屏幕时预览

<Snapshot path="capture-screen/examples/preview" lang="tsx" />

## 自定义保存录制视频方法

<Snapshot path="capture-screen/examples/custom-save" lang="tsx" />

import MBox from '../markdown';
import api from './examples/api.md?raw';

<MBox text={api} />
