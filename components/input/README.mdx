export const basic = {
  type: '数据录入',
  title: '输入框',
  subtitle: 'Input',
};

# 输入框

> 在页面中输入文字内容

import Snapshot from '@/components/snapshot';

## 简单使用

<Snapshot path="input/examples/default" lang="tsx" cols={5} />

import MBox from '../markdown';

import api from './examples/api.md?raw';

<MBox text={api} />
