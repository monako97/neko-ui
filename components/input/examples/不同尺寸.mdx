<div style={{ width: 375 }}>
  {['normal', 'small', 'large'].map((item) => {
    return (
      <div key={item} style={{ marginBottom: 8 }}>
        <label htmlFor={item}>{item}: </label>
        <Input id={item} size={item} placeholder={'请输入'} prefix={'😊'} suffix={'🔚'} />
      </div>
    );
  })}
</div>
