import { WaveCircle } from 'neko-ui';
import { FC } from 'react';

const Example: FC = () => {
  return (
    <div style={{ padding: 100 }}>
      <WaveCircle />
    </div>
  );
};

export default Example;
