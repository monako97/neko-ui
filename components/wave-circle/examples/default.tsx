import { WaveCircle } from 'neko-ui';
import React, { FC } from 'react';

const Example: FC = () => {
  return (
    <div style={{ padding: 100 }}>
      <WaveCircle />
    </div>
  );
};

export const title = '案例二';
export default Example;
