export const basic = {
  type: '组件',
  title: '水波纹',
  subtitle: 'WaveCircle',
};
import api from './examples/api.md?raw';
import code from './examples/案例一?raw';

<Box>

# 水波纹

> 波纹溢出动画, 用于标记用户在线状态

## 直接在 mdx 中写

<div className="n-flex n-h-72">
  <WaveCircle bgColor="pink" />
  <WaveCircle>默认</WaveCircle>
</div>

```tsx
<div className="n-flex n-h-72">
  <WaveCircle bgColor="pink" />
  <WaveCircle>默认</WaveCircle>
</div>
```

### 读取文件并只展示代码 `./examples/default.tsx`

<CodeBlock code={code} lang="tsx" />

> 或传入 path (src 目录下代码所在位置) 通过 Snapshot 展示

<Snapshot name="wave-circle" />
</Box>

<Box text={api} />
