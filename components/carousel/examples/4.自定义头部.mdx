```tsx
<Carousel dots header={(num) => <div style={{ padding: 16 }}>1000 / {num + 1}</div>}>
  {Array(1000)
    .fill(0)
    .map((_, i) => {
      return <div key={i}>1000 / {i + 1}</div>;
    })}
</Carousel>
```
