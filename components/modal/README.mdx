---
type: 反馈
title: 模态框
subtitle: n-modal
icon: ☱
---

# Modal 模态对话框

> 需要用户处理事务，又不希望跳转页面以致打断工作流程时，可以使用 `模态框` 在当前页面正中打开一个浮层，在浮层中进行操作。

## 注册组件 `只需注册一次`

```js
import { Modal, registry } 'neko-ui';

registry(Modal);
```
